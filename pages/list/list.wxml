<view class="container root">
    <block wx:for="{{datas}}" wx:for-item="data" wx:key="data.id">
        <view class="item" id="{{index+currSize}}" bindtap="clickShowContent">{{data}}</view>
    </block>
</view>